# Production dependencies only (no dev tools, no duplicates)
# Use this for Docker builds to minimize image size

# Core ML dependencies
insightface==1.0.1
onnxruntime==1.29.0
numpy==2.5.2
opencv-python-headless==5.0.0.93

# Web framework (async)
quart==0.23.0
uvicorn[standard]==0.52.4

# HTTP client for the image_url input
httpx==0.28.1

# Data validation
pydantic==2.13.5
pydantic-settings==2.15.0

# Monitoring
newrelic==13.5.0

# Used directly by src/services/pipeline.py
Pillow==12.3.0

# Required by insightface (transitive but pinned for reproducibility)
tqdm==4.70.0
